export class FaceMapping {
	/**
	 * @param {number} sourceIdx
	 * @param {number} targetIdx
	 */
	constructor(sourceIdx, targetIdx) {
		/** @type {number} */
		this.source_idx = sourceIdx
		/** @type {number} */
		this.target_idx = targetIdx
	}
}
